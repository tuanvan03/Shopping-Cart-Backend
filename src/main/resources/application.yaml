spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopping
    username: root
    password: root@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
